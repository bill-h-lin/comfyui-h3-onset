[project]
name = "comfyui-h3-onset"
description = "Two nodes for MiniMax H3 audio: remove the phantom onset it copies from your reference, and command WHEN it makes a sound."
version = "2.0.0"
license = { text = "MIT" }
dependencies = []
requires-python = ">=3.10"

[project.urls]
Repository = "https://github.com/bill-h-lin/comfyui-h3-onset"

[tool.comfy]
PublisherId = "bill-h-lin"
DisplayName = "H3 Onset"
Repository = "https://github.com/bill-h-lin/comfyui-h3-onset"
